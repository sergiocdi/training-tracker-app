import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.velocity.tracker',
  appName: 'Velocity',
  webDir: 'dist/training-tracker/browser'
};

export default config;
